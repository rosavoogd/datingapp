<h1>Animals</h1>
<% for (var index = 0; index < data.length; index++){ %>
    <h2>   
        <a href="/<%= data[index].name %>"><%= data[index].species%></a>
    </h2>
    <p><%= data[index].bio %></p>
    <p><%= data[index].age %></p>
    <% } %>
 
